{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2022",                                 /* Set the JavaScript language version to target modern runtime environments (e.g., Node.js >= 16). */
    "module": "ES2022",                                 /* Use ES module syntax for output generation. */
    "rootDir": "src",                                   /* Specify the root folder for source files. */
    "outDir": "dist",                                   /* Specify the output folder for compiled files. */
    "moduleResolution": "bundler",                        /* Resolve module imports like Node.js does. */
    "experimentalDecorators": true,                    /* Enable use of experimental decorators. */
    "importHelpers": true,                              /* Avoid duplication of helper functions in output by importing them from tslib. */
    "esModuleInterop": true,                            /* Allow easier interoperability with CommonJS modules by adding a small interop wrapper. */
    "forceConsistentCasingInFileNames": true,           /* Catch case sensitivity issues in imports between operating systems. */
    "skipLibCheck": true,                               /* Skip checking types in `.d.ts` files for better performance. */

    /* Type Checking */
    "strict": true                                      /* Enable all strict type-checking options to enforce better code quality. */
  },
  "include": ["src/**/*"],                              /* Include all TypeScript files in the `src/` folder. */
  "exclude": ["node_modules", "dist"]                   /* Exclude dependencies and output folders from compilation. */
}